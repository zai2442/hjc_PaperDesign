```mermaid
graph TD
    subgraph 学生用户
        A[浏览活动列表] --> B{发现感兴趣的活动？};
        B -- 是 --> C[查看活动详情];
        C --> D{决定报名？};
        D -- 是 --> E[提交报名申请];
        E --> F[等待审核];
        F --> G{审核通过？};
        G -- 是 --> H[收到成功通知];
        H --> I[活动开始前到达现场];
        I --> J[扫描签到二维码];
        J --> K{签到成功？};
        K -- 是 --> L[参与活动];
        B -- 否 --> A;
        D -- 否 --> A;
        G -- 否 --> M[收到失败通知];
        K -- 否 --> J;
    end

    subgraph 活动管理方 (社团/管理员)
        O[登录后台] --> P[发布/管理活动];
        P --> Q[接收学生报名申请];
        Q --> R[审核报名];
        R -- 通过 --> F;
        R -- 驳回 --> M;
        S[活动开始时] --> T[生成动态签到码];
        T --> J;
        U[活动结束后] --> V[查看数据统计];
    end
```
